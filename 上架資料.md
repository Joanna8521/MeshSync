# Chrome 線上應用程式商店 — 上架填寫內容

上傳檔案：`mesh-sync-store-v3.0.0.zip`（專案根目錄）
後台網址：<https://chrome.google.com/webstore/devconsole>

> 上傳的是**商店版**套件，由 `python3 tools/build_store_zip.py` 產生。
> 它比本機版少了 manifest 的 `key` 欄位（商店不接受該欄位，ID 由商店指派）。
> 本機載入測試請照舊用 `extension/` 資料夾。

---

## 一、商店資訊

**名稱**（最多 75 字）

```
Mesh Sync — AI 對話脈絡收藏器
```

**簡短說明**（最多 132 字，會顯示在搜尋結果）

```
把 ChatGPT / Claude / Gemini 的重要討論存進你自己的 Google Drive。一場對話一份 Doc，自動命名，底部附原始討論連結。
```

**詳細說明**

```
和 AI 討論出來的好東西，常常散在幾十場對話裡，隔天就找不回來了。
Mesh Sync 讓你在對話當下把重要的段落收起來，寫進你自己的 Google Drive。

【三種收錄方式】
1. 圈選文字：選取重要段落，旁邊浮出「存入脈絡」按鈕，按一下就收好。
2. AI 標記：把一段 prompt 貼進 AI 的個人化設定，對它說「記錄脈絡」，
   它會整理出總結、背景、重點、原話、決定、待解問題、下一步，
   擴充偵測到就跳出確認卡，可以連同前後對話原文一起寫入。
3. 整場對話：一鍵把完整問答依序收錄成逐字稿。

【收進來的樣子】
・一場對話一份 Google Doc，同一場存幾次都累加在同一份，各自附時間戳
・檔名自動命名為「日期_主題_平台」，例如 20260813_品牌定位討論_ChatGPT
・文件底部固定附上原始討論的連結，隨時跳回當時的對話
・可依客戶／專案切換，寫入不同的資料夾

【關於你的資料】
・完全沒有伺服器。內容從你的瀏覽器直接寫進你自己的 Google Drive。
・只要求 drive.file 權限，這個擴充只能存取它自己建立的資料夾與文件，
  看不到你雲端硬碟裡的其他任何檔案。
・所有收錄都要你按下按鈕才會寫入，不會自動上傳任何內容。
・完整開源，程式碼可自行查證：https://github.com/Joanna8521/MeshSync
```

**類別**：生產力工具（Productivity）
**語言**：中文（繁體）

---

## 二、隱私權做法（審查最重視這一區）

**單一用途說明**

```
把使用者在 AI 對話網站上選定的討論內容，寫入他自己的 Google Drive 文件保存。
```

**權限用途說明**（逐項填寫）

| 權限 | 說明 |
|---|---|
| `storage` | 儲存使用者的客戶設定、資料夾名稱與偏好，全部存在本機。 |
| `identity` | 讓使用者以自己的 Google 帳號授權，取得寫入自己 Drive 的權杖。 |
| `chatgpt.com` `chat.openai.com` `claude.ai` `gemini.google.com` | 在這些對話頁面顯示擷取按鈕、偵測使用者要求 AI 輸出的脈絡標記，並在使用者按下按鈕時讀取他選定的內容。 |
| `googleapis.com` `docs.googleapis.com` | 呼叫 Google Drive 與 Docs API，把內容寫進使用者自己的雲端硬碟。 |

**遠端程式碼**：否（所有程式碼都包在套件內）

**資料用途聲明**（逐項勾選並說明）

- 蒐集「網站內容」：是 — 僅限使用者主動選取並按下按鈕的段落，直接寫入他本人的 Google Drive。
- 不會出售給第三方、不用於與核心功能無關的用途、不用於判斷信用度或放貸。

**隱私權政策網址**

```
https://joanna8521.github.io/MeshSync/privacy.html
```

---

## 三、圖片素材

必備：**1280×800 或 640×400 的螢幕擷圖至少一張**（最多五張）。建議三張：

1. 對話頁面上跳出脈絡確認卡的樣子
2. popup 介面（客戶切換、收錄設定）
3. 寫進 Google Doc 的成果（標題、時間戳、底部連結）

商店圖示 128×128 會自動取用套件裡的 `icons/icon128.png`。

---

## 四、上架後必做（重要，順序不能顛倒）

上架後商店會指派一個**新的擴充 ID**（跟本機測試用的
`mgeigmfkagbfbgngheecjlcdiemajpdg` 不同），OAuth 必須跟著更新，
否則商店版按「連接 Google」會失敗。

1. 在後台的項目頁面複製**商店指派的 ID**。
2. 到 Google Cloud Console → 憑證 → 建立**第二個** OAuth 用戶端
   （類型一樣選「Chrome 擴充功能」，項目 ID 填商店那個），
   保留原本那組給本機測試用。
3. 用新的 client ID 重新打包後更新版本：

   ```bash
   python3 tools/build_store_zip.py --client-id 新的ID.apps.googleusercontent.com
   ```

4. 把重新產生的 zip 上傳為新版本。
5. 回 OAuth 憑證頁面把「驗證應用程式擁有權」補完（上架後才做得到），
   使用者的授權畫面就不會出現額外警告。

---

## 五、能見度建議

先選 **「未列出」（Unlisted）**：一樣要過審查、一樣有自動更新，憑連結就能安裝，
但搜尋不到。適合先給學員用，之後想公開再切換成「公開」即可。
